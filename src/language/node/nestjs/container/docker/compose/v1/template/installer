services:
    ~~~service name~~~:
        container_name: ~~~container name~~~
        build:
            context: ./
            dockerfile: Dockerfile
        image: ~~~image name~~~
        restart: "no"
        ports:
            - "~~~host port~~~:3000"
        volumes:
            - ./:/usr/src/app